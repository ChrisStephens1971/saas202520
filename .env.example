# Tournament Platform - Environment Configuration
# Copy this to .env.local for local development

# ============================================
# DATABASE (REQUIRED)
# ============================================

# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database_name
# For local development, use default PostgreSQL credentials
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tournament_platform"

# ============================================
# AUTHENTICATION (REQUIRED)
# ============================================

# NextAuth Secret - Generate with: openssl rand -base64 32
# IMPORTANT: Change this in production!
AUTH_SECRET="your-secret-key-here-change-in-production"

# Application URL
# For local dev: http://localhost:3000
# For production: your actual domain
NEXTAUTH_URL="http://localhost:3000"

# ============================================
# APPLICATION
# ============================================

# Node environment (development | production | test)
NODE_ENV="development"

# Server port (default: 3000)
PORT=3000

# Public app URL (used in emails, notifications)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Socket.IO URL (optional, defaults to same origin)
# NEXT_PUBLIC_SOCKET_URL="http://localhost:3000"

# ============================================
# EMAIL / SMTP (OPTIONAL - Required for notifications)
# ============================================

# SMTP Server Configuration
# For Gmail: Use app-specific password (https://support.google.com/accounts/answer/185833)
# For testing: Use services like Mailtrap or Ethereal
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-specific-password"
SMTP_FROM='"Tournament Platform" <noreply@tournament.com>'

# ============================================
# REDIS (OPTIONAL - For caching and rate limiting)
# ============================================

# Local Redis (for caching)
REDIS_HOST="localhost"
REDIS_PORT="6379"
REDIS_PASSWORD=""
REDIS_DB="0"

# Upstash Redis (for serverless rate limiting)
# Get free tier at: https://console.upstash.com/
# UPSTASH_REDIS_REST_URL="https://your-redis-instance.upstash.io"
# UPSTASH_REDIS_REST_TOKEN="your-redis-token"

# ============================================
# STRIPE (OPTIONAL - For payment processing)
# ============================================

# Stripe API Keys (use test keys for development)
# Get from: https://dashboard.stripe.com/test/apikeys
# STRIPE_SECRET_KEY="sk_test_..."
# STRIPE_PUBLISHABLE_KEY="pk_test_..."
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."

# ============================================
# TWILIO (OPTIONAL - For SMS notifications)
# ============================================

# Note: Twilio credentials are stored per-organization in the database
# These environment variables are only for development/testing
# Get from: https://console.twilio.com/
# TWILIO_ACCOUNT_SID="ACxxxxxxxxxxxxx"
# TWILIO_AUTH_TOKEN="your-auth-token"
# TWILIO_PHONE_NUMBER="+1234567890"

# ============================================
# MONITORING (OPTIONAL - For production)
# ============================================

# Sentry (Error tracking)
# Get from: https://sentry.io/
# NEXT_PUBLIC_SENTRY_DSN="https://your-sentry-dsn"
# SENTRY_ORG="your-org"
# SENTRY_PROJECT="your-project"

# Performance metrics endpoint
# METRICS_ENDPOINT="https://your-metrics-endpoint.com/api/metrics"

# ============================================
# OFFLINE/SYNC FEATURES (DEFERRED TO V2)
# ============================================

# Offline-first sync service (WebSocket CRDT synchronization)
# Status: Designed but disabled for V1 - will be enabled in future release
# Leave this set to "false" for the initial release
OFFLINE_SYNC_ENABLED="false"

# Sync service URL (only used when OFFLINE_SYNC_ENABLED=true)
# SYNC_SERVICE_URL="http://localhost:4000"

# ============================================
# NOTES
# ============================================

# REQUIRED for local development:
# - DATABASE_URL (PostgreSQL)
# - AUTH_SECRET
# - NEXTAUTH_URL

# OPTIONAL for full features:
# - SMTP_* (for email notifications)
# - REDIS_* (for caching and performance)
# - STRIPE_* (for payment processing)
# - TWILIO_* (for SMS notifications)

# For a minimal working setup, you only need the REQUIRED variables above.
# See docs/LOCAL_DEV.md for detailed setup instructions.
